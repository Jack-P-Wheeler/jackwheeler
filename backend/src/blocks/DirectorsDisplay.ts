import { Block } from 'payload/types';

const DirectorsDisplay: Block = {
    slug: 'DirectorsDisplay',
    imageURL: 'http://localhost:3000/media/hero.png',
    imageAltText: 'Directors block preview',
    fields: [
        
    ]
};

export default DirectorsDisplay